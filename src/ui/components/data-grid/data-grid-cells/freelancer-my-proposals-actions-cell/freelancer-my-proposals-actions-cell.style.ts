import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles()(() => ({
  proposalsActions: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    gap: '10px',
  },
}))
