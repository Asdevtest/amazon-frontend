import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles()(() => ({
  ideaWrapper: {
    padding: '10px 0 ',
    width: '100%',
  },

  ideaProductActionButton: {
    width: '100%',
  },
}))
