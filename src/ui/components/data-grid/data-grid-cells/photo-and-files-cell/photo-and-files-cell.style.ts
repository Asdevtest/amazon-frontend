import { makeStyles } from 'tss-react/mui'

export const useDataGridCellStyles = makeStyles()(() => ({
  photoWrapper: {
    display: 'flex',
    alignItems: 'center',
    padding: '10px 0',
  },
}))
