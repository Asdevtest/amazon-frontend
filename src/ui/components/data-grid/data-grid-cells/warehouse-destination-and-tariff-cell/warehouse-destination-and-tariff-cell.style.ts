import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles()(() => ({
  destinationAndTariffWrapper: {
    padding: '20px 0',
  },

  storekeeperBtn: {
    width: 160,
    marginTop: 10,
    whiteSpace: 'normal',
    minHeight: '40px',
    height: 'unset',
  },
}))
