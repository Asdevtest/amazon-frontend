import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles()(() => ({
  radioButtonsWrapper: {
    width: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-start',
    padding: '0 20px',
  },
}))
