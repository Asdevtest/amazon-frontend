import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles()(() => ({
  mainFilterBtnInsert: {
    display: 'flex',
  },

  mainFilterBtnInsertText: {
    fontWeight: 600,
    marginLeft: 5,
  },
}))
