import {makeStyles} from '@material-ui/core'

export const useClassNames = makeStyles(() => ({
  root: {
    width: '100%',
    overflow: 'auto',
    height: '100%',
    padding: '10px 20px',
  },
}))
