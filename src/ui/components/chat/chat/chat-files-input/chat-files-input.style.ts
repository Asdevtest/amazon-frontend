import {makeStyles} from '@material-ui/core'

export const useClassNames = makeStyles(() => ({
  root: {
    width: '100%',
    overflow: 'scroll',
    height: '100%',
    padding: '0 20px',
  },
}))
