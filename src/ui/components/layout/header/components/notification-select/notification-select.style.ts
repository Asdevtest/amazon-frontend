import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles()(() => ({
  root: {
    width: '28px',
    height: '28px',
    padding: 0,
  },

  icon: {
    width: '28px',
    height: '28px',
  },
}))
