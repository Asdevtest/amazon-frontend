import { observer } from 'mobx-react'

import { useStyles } from './edit-product-tag.style'

export const EditProductTag = observer(() => {
  const { classes: styles } = useStyles()

  return null
})
