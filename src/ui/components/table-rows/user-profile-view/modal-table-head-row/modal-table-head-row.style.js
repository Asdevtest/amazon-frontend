import {createStyles} from '@material-ui/core'

export const styles = createStyles({
  tableHeadTypography: {
    color: 'rgb(61, 81, 112)',
    fontWeight: 500,
  },
  tableHeadCell: {
    padding: '8px',
  },
})
