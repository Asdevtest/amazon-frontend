import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles()(() => ({
  toolbar: {
    padding: '0 10px',
  },

  tableTitle: {
    fontSize: 18,
    lineHeight: '25px',
    fontWeight: 600,
  },
}))
