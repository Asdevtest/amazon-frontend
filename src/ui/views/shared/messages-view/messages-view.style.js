import {createStyles} from '@material-ui/core'

export const styles = createStyles(() => ({
  chatWrapper: {
    width: '100%',
    height: '778px',
  },

  searchInput: {
    border: '1px solid #007bff',
    width: 250,
    height: 36,
  },

  searchContainer: {
    width: 'auto',
  },
}))
