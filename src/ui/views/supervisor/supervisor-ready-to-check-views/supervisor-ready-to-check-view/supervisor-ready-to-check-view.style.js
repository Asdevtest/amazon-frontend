import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles()(() => ({
  btnsWrapper: {
    marginBottom: 20,
    display: 'flex',
  },

  datagridWrapper: {
    height: '81vh',
    width: '100%',
  },
}))
