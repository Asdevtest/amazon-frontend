import {createStyles} from '@material-ui/core'

export const styles = createStyles(() => ({
  backButtonWrapper: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'end',
    marginBottom: '30px',
  },

  backButton: {
    padding: 0,
    margin: 0,
    height: '40px',
    width: '140px',
  },
}))
