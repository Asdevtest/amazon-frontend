import { action, computed, observable } from 'mobx'

export const observerConfig = {
  progressValue: observable,
  showProgress: observable,
  uploadedFiles: observable,
  showBarcodeOrHscodeModal: observable,
  showSetBarcodeModal: observable,
  productCardModal: observable,
  showIdeaModal: observable,
  showLinkRequestModal: observable,
  showConfirmModal: observable,
  showProductLaunch: observable,
  showRequestDesignerResultModal: observable,
  showRequestBloggerResultModal: observable,
  showMainRequestResultModal: observable,
  showOrderModal: observable,
  showSelectionSupplierModal: observable,
  showCommentsModal: observable,
  showAddOrEditSupplierModal: observable,
  readyImages: observable,
  ideaList: observable,
  selectedProduct: observable,
  currentBarcode: observable,
  currentHscode: observable,
  storekeepers: observable,
  destinations: observable,
  ordersDataStateToSubmit: observable,
  selectedIdea: observable,
  selectedIdeaId: observable,
  currentProduct: observable,
  productsToLaunch: observable,
  productId: observable,
  currentIdeaId: observable,
  currentProposal: observable,
  currentRequest: observable,
  paymentMethods: observable,
  isIdeaCreate: observable,

  userInfo: computed,
  platformSettings: computed,
  destinationsFavourites: computed,

  getDataForIdeaModal: action.bound,
  onClickVariationRadioButton: action.bound,
  getSuppliersPaymentMethods: action.bound,
  getStorekeepers: action.bound,
  statusHandler: action.bound,
  handleStatusToCheck: action.bound,
  handleStatusToReject: action.bound,
  setRejectStatusHandler: action.bound,
  handleStatusToSupplierSearch: action.bound,
  handleStatusToProductCreating: action.bound,
  handleStatusToAddingAsin: action.bound,
  handleStatusToFinished: action.bound,
  handleRestore: action.bound,
  onSubmitRemoveIdea: action.bound,
  onClickRequestId: action.bound,
  onClickBarcode: action.bound,
  onDoubleClickBarcode: action.bound,
  onDeleteBarcode: action.bound,
  showBarcodeOrHscode: action.bound,
  onClickSaveBarcode: action.bound,
  setSelectedProduct: action.bound,
  onClickProductModal: action.bound,
  onClickShowProduct: action.bound,
  editIdea: action.bound,
  onSubmitCreateProduct: action.bound,
  onClickSaveSupplierBtn: action.bound,
  onClickToOrder: action.bound,
  onClickCreateRequestButton: action.bound,
  onClickLinkRequestButton: action.bound,
  onClickCreateProduct: action.bound,
  onClickCloseIdea: action.bound,
  onClickProductLaunch: action.bound,
  onClickNextButton: action.bound,
  onSubmitCalculateSeekSupplier: action.bound,
  onSubmitSeekSupplier: action.bound,
  onClickResultButton: action.bound,
  unbindRequest: action.bound,
  onClickUnbindButton: action.bound,
  onConfirmSubmitOrderProductModal: action.bound,
  onSubmitOrderProductModal: action.bound,
  createOrder: action.bound,
  setDestinationsFavouritesItem: action.bound,
  onClickAddSupplierButton: action.bound,
}
