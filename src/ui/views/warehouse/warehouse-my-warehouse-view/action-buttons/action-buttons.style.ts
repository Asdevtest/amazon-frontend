import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles()(() => ({
  editBtn: {
    padding: '0 54px',
  },

  leftBtnsWrapper: {
    display: 'flex',
    flexDirection: 'row',
    gap: '10px',
  },
}))
